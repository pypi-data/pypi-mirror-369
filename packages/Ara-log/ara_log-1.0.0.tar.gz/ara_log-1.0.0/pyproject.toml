[project]
name = "Ara_log"
version = "1.0.0"
description = "A simple, minimalistic python logger using the `logging` library as its basis"
readme = "README.md"
authors = [{ name = "HanamileH", email = "hanamileh@gmail.com" }]
license = "MIT"

dependencies = []

requires-python = ">=3.7"

[project.urls]
Homepage = "https://github.com/hanamileh-DEV/Ara_log"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"