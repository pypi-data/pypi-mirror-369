[package]
name = "libhans_derive"
version = "0.1.1"                                                      # 确保版本号明确
edition = "2024"
authors = ["Yixing <yanjizhou312@163.com>"]
license = "Apache-2.0"
description = "libhans_derive is a procedural macro crate for libhans"

[lib]
proc-macro = true

[dependencies]
quote = "1.0" # 替换为明确的版本号
syn = "2.0"   # 替换为明确的版本号
