[project]
name = "horses"
version = "0.1.1"
description = ""
authors = [
    {name = "Alba Selinee Martinez Santana",email = "25005822@saultcollege.ca"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]

[tool.poetry]
packages = [{include = "horses", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
race = 'horses.cli:main'
