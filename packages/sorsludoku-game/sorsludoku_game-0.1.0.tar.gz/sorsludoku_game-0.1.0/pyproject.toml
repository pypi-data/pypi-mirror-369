[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sorsludoku-game"
version = "0.1.0"
authors = [
  { name="Melih Orslu", email="example@email.com" }
]
description = "Python ve Streamlit ile Sudoku oyunu"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "streamlit"
]