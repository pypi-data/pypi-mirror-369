[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nhlang"
version = "0.1.1"
description = "A simple interpreter for NH Language"
readme = "README.md"
license = {text = "MIT"}
authors = [
  {name = "Huy", email = "wuocwee@gmail.com"}
]
requires-python = ">=3.7"

[project.scripts]
nhlang = "nhlang.interpreter:main"
