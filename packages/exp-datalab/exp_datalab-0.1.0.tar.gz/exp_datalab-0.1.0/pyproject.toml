[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "exp-datalab"
version = "0.1.0"
description = "Experimental tools for scientific calculations, including error propagation and data analysis."
readme = "README.md"
authors = [
    { name = "Pablo Castro Pérez" }
]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = ["numpy>=1.22"]

[project.urls]
"Homepage" = "https://github.com/yourusername/exp_datalab"
"Bug Tracker" = "https://github.com/yourusername/exp_datalab/issues"

[tool.setuptools.packages.find]
where = ["src"]
