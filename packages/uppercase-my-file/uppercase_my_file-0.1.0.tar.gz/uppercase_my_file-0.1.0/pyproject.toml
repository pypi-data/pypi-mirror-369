[project]
name = "uppercase-my-file"
version = "0.1.0"
description = ""
authors = [
    {name = "rubyocatesc",email = "19074179@saultcollege.ca"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]

[tool.poetry]
packages = [{include = "uppercase_my_file", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
upperfile = 'uppercase_my_file.cli:main'