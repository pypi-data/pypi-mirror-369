repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.5.0 # Use the same version as in pyproject.toml or a compatible one
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format
