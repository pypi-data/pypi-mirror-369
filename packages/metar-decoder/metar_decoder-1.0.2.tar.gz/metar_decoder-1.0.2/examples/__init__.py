"""
Paquete de ejemplos para METAR Decoder.

Contiene scripts de demostración que muestran cómo usar la clase `MetarDecoder`
y sus métodos (`to_dict()` y `to_flat_dict()`), así como el manejo de los tipos 
de datos (`Temperature`, `Wind`, `Pressure`, etc.).

Ejecutar ejemplo principal:
    python -m metar_decoder.examples.example
"""
