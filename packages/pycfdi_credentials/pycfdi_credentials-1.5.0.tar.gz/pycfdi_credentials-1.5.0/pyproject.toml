[project]
name = "pycfdi_credentials"
version = "1.5.0"
description = "Library to manage CSD and FIEL files from SAT. Use this to sign, verify and get certificate data."
authors = [
	{ name = "Moises Navarro", email = "moisalejandro@gmail.com" }
]
license = "LGPL-2.1"


readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pyopenssl>=25.1.0"]

[dependency-groups]
dev = ["hypothesis>=6.138.1", "pytest>=8.4.1", "pytest-cov>=6.2.1"]
