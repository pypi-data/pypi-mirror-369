[project]
name = "mlops-quality-assurance"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nbformat>=5.10.4",
    "pytest>=8.4.1",
    "typer>=0.16.0",
]


[project.scripts]
mlops-library-checker = "achs_mlops_quality.cli:app"  # ← console command

[tool.hatch.build.targets.wheel]
packages = ["src/achs_mlops_quality"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"



