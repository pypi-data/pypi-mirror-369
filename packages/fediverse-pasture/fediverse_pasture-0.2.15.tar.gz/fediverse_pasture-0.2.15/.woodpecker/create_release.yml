when:
  branch: [main]
  event: [push]

depends_on: ["test"]

steps:
  create_release:
    image: helgekr/release_helper
    environment:
      CODEBERG_TOKEN: { from_secret: codeberg_token }
      CODEBERG_OWNER: funfedidev
      CODEBERG_REPO: python_fediverse_pasture
      caddle_drive_connection_string:
        { from_secret: roboherd_connection_string }
