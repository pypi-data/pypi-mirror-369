[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "localtest"
version = "1.0.0"
description = "Local network speed analyzer and many more system analyzation tools."
authors = [
    { name = "Sabio Tang" }
]
dependencies = []
requires-python = ">=3.8"

[project.scripts]
localtest = "localtest.cli:main"