[project]
name = "syntopic"
version = "0.0.1"
description = "A placeholder package for SynTopic."
readme = "README.md"
authors = [
  { name = "Shoichi Seto", email = "seto-shoichi@syntopic.io" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
