[project]
name = "hulin-mcp-server"
version = "1.1.0"
description = "this is my test function,add two number and return result"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.12.4",
]

[project.scripts]
hulin-mcp-server = "hulin_mcp_server:main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
