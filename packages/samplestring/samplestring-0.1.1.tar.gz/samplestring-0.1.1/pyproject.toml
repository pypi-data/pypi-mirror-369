[project]
name = "samplestring"
version = "0.1.1"
description = "Easy sample for string"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
samplestring = "samplestring:main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
