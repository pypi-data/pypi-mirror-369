[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "digilog"
version = "0.0.1"
description = "Placeholder package. Name reserved for upcoming release."
readme = "README.md"

[tool.hatch.build.targets.wheel]
packages = ["src/digilog"]


