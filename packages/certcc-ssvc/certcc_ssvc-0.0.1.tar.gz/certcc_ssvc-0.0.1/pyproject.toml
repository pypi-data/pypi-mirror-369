[project]
name = "certcc-ssvc"
version = "0.0.1"
description = "Placeholder for the official CERTCC SSVC package (to be released around September 2025)"
authors = [
    { name = "CERT Coordination Center (CERTCC)", email = "cert@cert.org" }
]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"

[project.urls]
Homepage = "https://github.com/YOUR_GITHUB_USERNAME/ssvc"
