[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hello-sdk171"  # The name on PyPI
version = "0.1.1"
description = "A simple example Python SDK"
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
authors = [
    { name = "Rohit", email = "rohit.selvakumaran@gmail.com" }
]
keywords = ["sdk", "example", "hello"]
dependencies = []

[project.urls]
Homepage = "https://github.com/RO-HIT17/Python_SDK"
