# pyproject.toml

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "harmonify-patcher"
version = "3.2.0"
description = "Method behavior modification at runtime, made easy."
readme = "README.md"
authors = [{ name = "Wind Rider", email = "abcbcalol69@gmail.com" }]
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
keywords = ["patcher", "python", "library"]
dependencies = []
requires-python = ">=3.10"

[project.urls]
Homepage = "https://github.com/ProgrammingLover71/Harmonify"
