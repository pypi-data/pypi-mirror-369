[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nx_hif"
version = "0.0.8"
description = "HIF for NetworkX"
dependencies = [
  "networkx",
]

[project.urls]
"Source" = "https://github.com/colltoaction/nx_hif"
