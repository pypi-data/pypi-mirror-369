[project]
name = "kieran-mcp-upload-pypi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "bruan", email = "1441584221@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.13.0",
]

[project.scripts]
kieran-mcp-upload-pypi = "kieran_mcp_upload_pypi:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# 换源
[tool.uv]
index-url = "https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple"
