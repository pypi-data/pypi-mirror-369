[project]
name = "argus-logging"
version = "2.3.3"
description = "A module to simplify logging"
authors = [
    {name = "Michael Knowles"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]
keywords = ["python", "logging"]

[project.urls]
Source = "https://github.com/mapledyne/argus-logging/"
Documentation = "https://mapledyne.github.io/argus-logging/"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "argus-logging"
packages = [
    {include = "argus", from = "src"}
]


[tool.ruff]
line-length = 120
indent-width = 4

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.ruff.lint]
select = [
    "F",   # pyflakes
    "E",   # pycodestyle
    "I",   # isort
    "ANN", # flake8 type annotations
    "RUF", # ruff-specific rules
]
fixable = ["ALL"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
python_version = "3.12"
warn_return_any = true
disallow_untyped_defs = true