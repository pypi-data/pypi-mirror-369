[project]
name = "jker-mcp-publish-demo"
version = "0.1.2"
description = "use the latest one"
readme = "README.md"
authors = [
    { name = "Jker", email = "2933385376@qq.com" }
]
requires-python = ">=3.13.3"
dependencies = [
    "mcp[cli]>=1.13.0",
]

[project.scripts]
jker-mcp-publish-demo = "jker_mcp_publish_demo:main"

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"
