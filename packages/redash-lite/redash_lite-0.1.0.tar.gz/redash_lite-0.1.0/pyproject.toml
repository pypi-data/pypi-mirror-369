[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "redash_lite"
version = "0.1.0"
description = "Tiny client to trigger Redash queries and fetch fresh results"
authors = [
    { name = "Your Name", email = "you@example.com" }
]
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests"
]
