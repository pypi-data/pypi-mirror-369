Filters = [
  # we put non-binary files into /usr/lib/nagios
  "only-non-binary-in-usr-lib",
  # we depend on internal packages
  "requires-on-release nagios-plugins-igwn",
  "requires-on-release python3-igwn-monitor",
]
