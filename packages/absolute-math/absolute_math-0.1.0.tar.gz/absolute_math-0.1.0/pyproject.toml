[project]
name = "absolute_math"
version = "0.1.0"
description = "Python module that tests number characters with symbolic passages"
authors = [
    { name="MODUPY" }
]
dependencies = [
    "sympy>=1.12"
]
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
