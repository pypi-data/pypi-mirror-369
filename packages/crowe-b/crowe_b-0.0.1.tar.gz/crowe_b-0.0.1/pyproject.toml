[project]
name = "crowe-b"
version = "0.0.1"               # Increment this if re-uploading
description = "Crowe-B CLI (chat via api.crowe-ai.com)"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["requests>=2.28"]

[project.scripts]
crowe = "crowe_b.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
