[project]
name = "logos-cdi"
version = "1.1.0"
description = "The hackable dependency injection framework"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.uv]
package=true

[project.scripts]
logos = "logos_cdi.command:main"
