version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.13"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

sphinx:
  configuration: doc/conf.py