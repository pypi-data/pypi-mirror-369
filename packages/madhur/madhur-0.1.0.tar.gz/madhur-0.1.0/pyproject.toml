[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "madhur"  # This is the name you'll use in pip install
version = "0.1.0"
description = "My custom Python library"
authors = [{ name = "Madhur", email = "madhur@gmail.com" }]
license = { text = "MIT" }
dependencies = []
