[project]
name = "determystic"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [ "rich>=13.0.0", "click>=8.1.0", "ruff>=0.7.0", "ty", "pydantic-ai>=0.0.14", "anthropic>=0.39.0", "pytest>=8.0.0", "pytest-asyncio>=0.24.0", "pyyaml>=6.0.0", "pydantic-settings>=2.0.0", "tomli-w>=1.0.0", "prompt_toolkit>=3.0.0",]

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
determystic = "determystic.entrypoint:cli"

[tool.uv]
package = true
