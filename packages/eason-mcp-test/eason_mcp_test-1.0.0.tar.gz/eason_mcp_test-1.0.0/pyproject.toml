[project]
name = "eason-mcp-test"
version = "1.0.0"
description = "add calculator"
readme = "README.md"
authors = [
    { name = "Yichen Yao", email = "eason.yyc@inftech.ai" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.12.4",
]

[project.scripts]
eason-mcp-test = "eason_mcp_test:main"

[build-system]
requires = ["uv_build>=0.8.10,<0.9.0"]
build-backend = "uv_build"
