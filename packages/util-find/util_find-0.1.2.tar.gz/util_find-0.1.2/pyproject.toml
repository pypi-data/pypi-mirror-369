[project]
name = "util-find"
version = "0.1.2"
description = "A utility for finding files and directories"
readme = "README.md"
requires-python = ">=3.8"
license-files = ["LICENSE"]
authors = [
    {name = "birds", email = "cg626@163.com"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
]
dependencies = []
[project.urls]
git = "https://gitee.com/mpypi/util-find"

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

