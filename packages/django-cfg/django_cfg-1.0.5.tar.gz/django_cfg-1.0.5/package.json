{
  "name": "django-cfg",
  "version": "1.0.0",
  "description": "**Django Config Toolkit**",
  "license": "ISC",
  "author": "Unrealos",
  "scripts": {
    "commit": "npx openrouter-commit -run",
    "commit2": "git add . && git commit -m 'fixes' && git push",
    "cli": "poetry run python scripts/dev_cli.py"
  }
}
