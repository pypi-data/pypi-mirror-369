[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "django-zero-migrations-commands"
version = "1.0.3"
authors = [
  { name="Julian Bürklin", email="jb@software-design.de" },
]
description = "Management commands for creating and applying zero migrations."
readme = "README.md"
requires-python = ">=3.6"
dependencies = []
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://gitlab.com/software-design-public/django-zero-migrations-commands"
Issues = "https://gitlab.com/software-design-public/django-zero-migrations-commands/-/issues"
