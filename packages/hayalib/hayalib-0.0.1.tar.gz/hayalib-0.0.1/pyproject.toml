[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hayalib"
version = "0.0.1"
description = "哈基密文API库"
authors = [{ name="WIFI连接超时", email="localaccount@qq.com" }]
license = { text = "GPLv3" }
readme = "README.md"
requires-python = ">=3.8"