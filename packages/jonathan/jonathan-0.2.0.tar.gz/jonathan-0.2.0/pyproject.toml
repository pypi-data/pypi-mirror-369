[project]
name = "jonathan"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jonathan343", email = "jonathangee09@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
jonathan = "jonathan:main"

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"
