[project]
name = "iflow-mcp-hotels-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["mcp[cli]>=1.2.0", "httpx", "python-dotenv"]

[project.scripts]
hotels-mcp-server = "hotels_mcp.hotels_server:main"

[tool.uv]
package = true