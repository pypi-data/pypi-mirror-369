# Test configuration for Vault integration tests
vault:
  url: "http://localhost:8200"
  token: "test-token"
  secret_engine: "secret"
  path_prefix: "test-env"

env_file:
  path: "tests/test.env"
  encoding: "utf-8"

verbose: true
dry_run: false
