[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "textutils-plus"  # This is the PyPI name
version = "0.1.0"
description = "A small library for counting words and reversing text."
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Your Name", email = "you@example.com"}
]
requires-python = ">=3.7"

[project.urls]
Homepage = "https://github.com/yourusername/textutils-plus"
