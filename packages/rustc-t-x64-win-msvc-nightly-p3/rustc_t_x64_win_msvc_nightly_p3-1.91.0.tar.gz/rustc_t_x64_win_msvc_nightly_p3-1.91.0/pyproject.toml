[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rustc_t_x64_win_msvc_nightly_p3"
version = "1.91.0"
description = "Segment rustc.target.x86_64-pc-windows-msvc part p3"
license = { text = "MIT" }

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["**/*"]
