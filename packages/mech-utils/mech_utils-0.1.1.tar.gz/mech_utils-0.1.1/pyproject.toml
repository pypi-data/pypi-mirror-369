[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "mech_utils"         # Must be unique on PyPI
version = "0.1.1"
description = "mech_2700_toolbox"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
authors = [{ name = "Ryan", email = "cashconvertryan@gmail.com" }]

[tool.hatch.build.targets.wheel]
packages = ["src/mech_utils"]
