[project]
name = "greenpulse-mcp"
version = "1.0.0"
description = "greenpulse MCP server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [ "mcp>=1.12.3", "aiohttp" ]

[build-system]
requires = [ "uv_build>=0.8.4,<0.9.0",]
build-backend = "uv_build"

[project.scripts]
greenpulse-mcp = "greenpulse_mcp:main"
