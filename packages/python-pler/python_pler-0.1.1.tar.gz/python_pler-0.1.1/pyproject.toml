[project]
name = "python-pler"
version = "0.1.1"
description = "Pler - Probe and Cloudflare Filter Tool"
authors = [
    { name = "Justakazh", email = "akaswisnuaji@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests"
]

[project.scripts]
pler = "pler.cli:main"  

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"