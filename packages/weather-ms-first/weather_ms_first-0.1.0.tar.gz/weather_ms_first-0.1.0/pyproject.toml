[project]
name = "weather-ms-first"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.4",
]

[tool.uv]
dev-dependencies = []

# 让安装包的人可以直接用 weather_ms_first 命令运行 CLI
[project.scripts]
weather-ms-first = "weather_ms_first.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/weather_ms_first"]
