[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "BDviz"
version = "1.0.1"

dependencies = [
 "numpy","astropy", "matplotlib", "astroquery", "ipympl", "IPython", "jupyter"
]
