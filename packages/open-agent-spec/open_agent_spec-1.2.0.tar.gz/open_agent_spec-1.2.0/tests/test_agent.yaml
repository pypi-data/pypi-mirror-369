info:
  name: test-agent
  description: A test agent

intelligence:
  endpoint: https://api.openai.com/v1
  model: gpt-4
  config:
    temperature: 0.7
    max_tokens: 1000

tasks:
  analyze:
    description: Analyze the given input
    input:
      text: string
    output:
      summary: string
      key_points: string
