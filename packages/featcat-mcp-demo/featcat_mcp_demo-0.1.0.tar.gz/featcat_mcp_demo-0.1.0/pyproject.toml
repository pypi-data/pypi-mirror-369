[project]
name = "featcat-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fatcat", email = "fatcat@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.12.4",
]

[project.scripts]
featcat-mcp-demo = "featcat_mcp_demo:main"

[build-system]
requires = ["uv_build>=0.8.9,<0.9.0"]
build-backend = "uv_build"
