[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[project]
name = "fastdkdc"
version = "0.1.1"
description = "fastdkdc"
readme = "README.md"
authors = [{ name = "Cody", email = "cody@dkdc.dev" }]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
fastdkdc = "fastdkdc:main"

[dependency-groups]
dev = ["pre-commit>=4.3.0", "ruff>=0.12.8"]
