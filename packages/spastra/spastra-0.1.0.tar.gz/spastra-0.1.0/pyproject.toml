[project]
name = "spastra"
version = "0.1.0"
description = ""
authors = [
    {name = "Ayoub G.",email = "dev@ayghri.com"}
]
readme = "README.md"
requires-python = ">=3.9,<3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "spastra", from = "./"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
